import { PagesExemploPipe } from './pages-exemplo.pipe';

describe('PagesExemploPipe', () => {
  it('create an instance', () => {
    const pipe = new PagesExemploPipe();
    expect(pipe).toBeTruthy();
  });
});
